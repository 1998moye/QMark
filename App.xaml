<Application x:Class="MarkdownEditor.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MarkdownEditor"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Theme-aware context menu brushes (updated by ApplyTheme) -->
        <SolidColorBrush x:Key="CtxMenuBgBrush" Color="#ffffff" />
        <SolidColorBrush x:Key="CtxMenuBorderBrush" Color="#e5e7eb" />
        <SolidColorBrush x:Key="CtxMenuTextBrush" Color="#1a1a2e" />
        <SolidColorBrush x:Key="CtxMenuHoverBgBrush" Color="#f3f4f6" />
        <SolidColorBrush x:Key="CtxMenuActiveBgBrush" Color="#e8ecf4" />
        <SolidColorBrush x:Key="CtxMenuDangerBrush" Color="#ef4444" />
        <SolidColorBrush x:Key="CtxMenuDangerHoverBrush" Color="#fee2e2" />

        <!-- Drop shadow for context menus -->
        <DropShadowEffect x:Key="CtxMenuShadow" BlurRadius="16" ShadowDepth="3" Opacity="0.16" Color="#000" />

        <!-- ContextMenu style: modern rounded popup with shadow -->
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource CtxMenuBgBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource CtxMenuBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="4" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContextMenu">
                        <Border x:Name="root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                Effect="{StaticResource CtxMenuShadow}">
                            <ItemsPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- MenuItem style: flat with hover highlight -->
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource CtxMenuTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Padding" Value="10,7,14,7" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="MenuItem">
                        <Border x:Name="bd" Background="Transparent" BorderThickness="0"
                                CornerRadius="5" Padding="{TemplateBinding Padding}" Margin="1,0">
                            <ContentPresenter ContentSource="Header" VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource CtxMenuHoverBgBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Separator in context menus -->
        <Style x:Key="MenuSep" TargetType="Separator">
            <Setter Property="Margin" Value="6,4" />
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="{DynamicResource CtxMenuBorderBrush}" />
            <Setter Property="Opacity" Value="0.5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Background}"
                                Height="{TemplateBinding Height}"
                                Opacity="{TemplateBinding Opacity}"
                                CornerRadius="0.5"
                                Margin="{TemplateBinding Margin}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Implicit Separator in ContextMenu: thin subtle line -->
        <Style TargetType="Separator">
            <Setter Property="Margin" Value="6,4" />
            <Setter Property="Height" Value="1" />
            <Setter Property="Background" Value="{DynamicResource CtxMenuBorderBrush}" />
            <Setter Property="Opacity" Value="0.5" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Separator">
                        <Border Background="{TemplateBinding Background}"
                                Height="{TemplateBinding Height}"
                                Opacity="{TemplateBinding Opacity}"
                                CornerRadius="0.5"
                                Margin="{TemplateBinding Margin}" />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
